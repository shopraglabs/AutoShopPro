# This file configures the Dart analyzer for AutoShopPro.
# We start from the Flutter recommended lint set, then add stricter rules
# that catch real bugs and enforce consistent style.

include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat these as errors (not just warnings) so CI catches them
    missing_return: error
    dead_code: warning
    unused_import: warning
    unused_local_variable: warning

linter:
  rules:
    # ── Style ────────────────────────────────────────────────────────────────
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_single_quotes: true

    # ── Safety ───────────────────────────────────────────────────────────────
    avoid_print: true          # use debugPrint or a logger instead
    avoid_dynamic_calls: true  # catches accidental .call() on untyped values
    avoid_returning_null_for_future: true
    cancel_subscriptions: true
    close_sinks: true

    # ── Code quality ─────────────────────────────────────────────────────────
    always_declare_return_types: true
    avoid_redundant_argument_values: true
    avoid_unused_constructor_parameters: true
    directives_ordering: true
    eol_at_end_of_file: true
    no_duplicate_case_values: true
    unawaited_futures: true    # catches fire-and-forget async calls
    use_if_null_to_convert_nulls_to_bools: true
    use_string_buffers: true
